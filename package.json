{
  "name": "game-loader",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for the @game-loader/react library and its demo site.",
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist .next .turbo && rm -rf node_modules"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
