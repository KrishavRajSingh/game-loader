import type { GameMeta } from "../../types.js";

export const dinoMeta: GameMeta = {
  id: "dino",
  displayName: "Dino Runner",
  description:
    "Press space (or tap) to jump. The classic Chrome offline runner, ported line-for-line from the upstream Runner.js.",
  tags: ["runner", "arcade", "1-button"],
  minSize: "sm",
};
